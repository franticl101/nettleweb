{
  "name": "nettleweb",
  "version": "2.0.0",
  "private": true,
  "description": "An open board where anyone in the company can post a Zoom or Google Meet link",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^11.1.0",
    "@netlify/functions": "^6.0.0",
    "@netlify/functions": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
